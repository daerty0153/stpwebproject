server { 
		listen 80; 
		server_name _;
		error_log /home/box/test.error.log;
		location ^~ /uploads/ { 
			root /home/box/web/uploads;
		}
		location ~* \.\.w+$ { 
			root /home/box/web/public; 
		}
		
		location ~* ^\w+$ {
			return 404;
		}
}
